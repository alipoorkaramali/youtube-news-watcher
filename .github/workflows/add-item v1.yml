name: Add Watch Item v.2➕✅

on:
  workflow_dispatch:
    inputs:
      platform:
        description: '🌐 پلتفرم'
        required: true
        type: choice
        default: 'youtube'
        options:
          - 'youtube'
          - 'soundcloud_playlist'
      channel_id:
        description: 'شناسه کانال یوتیوب (UC...) / لینک پلی‌لیست ساندکلاد'
        required: true
        type: string
      title_keyword:
        description: 'کلیدواژه ثابت در عنوان ویدیو/آهنگ'
        required: true
        type: string
      start_time_iran:
        description: 'ساعت شروع به وقت ایران (HH:MM)'
        required: true
        type: string
        default: '19:00'
      check_every_minutes:
        description: 'فاصلهٔ بین تلاش‌ها (دقیقه)'
        required: false
        type: number
        default: 60
      max_attempts:
        description: 'حداکثر تلاش در روز'
        required: false
        type: number
        default: 5

jobs:
  add:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Append or replace item in watchlist.json
        env:
          PLATFORM: ${{ github.event.inputs.platform }}
          CHANNEL_ID: ${{ github.event.inputs.channel_id }}
          TITLE_KEYWORD: ${{ github.event.inputs.title_keyword }}
          START_TIME: ${{ github.event.inputs.start_time_iran }}
          INTERVAL: ${{ github.event.inputs.check_every_minutes }}
          ATTEMPTS: ${{ github.event.inputs.max_attempts }}
        run: |
          NEW_ITEM=$(jq -n \
            --arg platform "$PLATFORM" \
            --arg cid "$CHANNEL_ID" \
            --arg kw "$TITLE_KEYWORD" \
            --arg st "$START_TIME" \
            --argjson iv "$INTERVAL" \
            --argjson ma "$ATTEMPTS" \
            '{platform: $platform, channel_id: $cid, title_keyword: $kw, start_time_iran: $st, check_every_minutes: $iv, max_attempts: $ma}')

          if [ ! -f watchlist.json ]; then
            echo "[$NEW_ITEM]" > watchlist.json
          else
            # حذف آیتم‌هایی که پلتفرم و کلیدواژه یکسان دارند
            jq --arg platform "$PLATFORM" --arg kw "$TITLE_KEYWORD" \
              'map(select(.platform != $platform or .title_keyword != $kw))' watchlist.json > tmp.json
            # اضافه کردن آیتم جدید
            jq ". += [$NEW_ITEM]" tmp.json > watchlist.json
            rm tmp.json
          fi
          echo "محتویات نهایی watchlist.json:"
          cat watchlist.json

      - name: Commit updated watchlist
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add watchlist.json
          if git diff --cached --quiet; then
            echo "No changes."
          else
            git commit -m "Add/replace watch item via form"
            git push
          fi
